name: Vercel Cron Scheduler via GitHub Actions

on:
  schedule:
    # 1. KIS Token Refresh (KST 15:40) -> UTC 06:40
    - cron: '40 6 * * *'
  workflow_dispatch:
    inputs:
      job_type:
        description: 'Choose the job to trigger manually'
        required: true
        default: 'kis_token_refresh'
        type: choice
        options:
          - kis_token_refresh

jobs:
  trigger-crons:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PRODUCTION_URL Secret
        run: |
          if [ -z "${{ secrets.PRODUCTION_URL }}" ]; then
            echo "WARNING: PRODUCTION_URL secret is not set in GitHub repository secrets."
            echo "Using fallback URL: https://k-stockreplay.pe.kr"
          fi

      - name: Trigger KIS Token Refresh
        if: |
          github.event.schedule == '40 6 * * *' ||
          (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_type == 'kis_token_refresh'))
        run: |
          PRODUCTION_URL="${{ secrets.PRODUCTION_URL }}"
          if [ -z "$PRODUCTION_URL" ]; then
            PRODUCTION_URL="https://k-stockreplay.pe.kr"
          fi
          
          echo "Triggering KIS Token Refresh at $PRODUCTION_URL/api/cron/kis-token-refresh ..."
          
          AUTH_HEADER=""
          if [ ! -z "${{ secrets.CRON_SECRET }}" ]; then
            AUTH_HEADER="Authorization: Bearer ${{ secrets.CRON_SECRET }}"
          fi
          
          RESPONSE=$(curl -s -w "\n%{http_code}" -X GET \
            -H "$AUTH_HEADER" \
            "$PRODUCTION_URL/api/cron/kis-token-refresh")
            
          HTTP_STATUS=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          echo "Response Body: $BODY"
          echo "HTTP Status Code: $HTTP_STATUS"
          
          if [ "$HTTP_STATUS" -ne 200 ] && [ "$HTTP_STATUS" -ne 201 ]; then
            echo "Failed to trigger KIS Token Refresh"
            exit 1
          fi
